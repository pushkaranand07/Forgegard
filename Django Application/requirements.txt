# ─────────────────────────────────────────────────────────────────────────────
# ForgeGuard — Python Dependencies (Windows Python 3.13 Compatible)
# 
# Install via: pip install -r requirements.txt
#
# ⚠️  CRITICAL: PyTorch MUST be installed via Conda FIRST!
#     
#     conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
#
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework
Django>=5.0.0
asgiref>=3.8.0
sqlparse>=0.5.0

# Data Science & Processing
numpy>=2.0.0,<3.0.0
pandas>=2.2.0
scipy>=1.12.0

# Image & Video Processing
opencv-python>=4.10.0
Pillow>=10.4.0
imageio>=2.25.0

# Deep Learning Models
# PyTorch via conda (see install instructions above)
# torch==2.3.1  ← Install via: conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
# torchvision==0.18.1  ← Install via: same conda command above
timm>=0.9.0  # PyTorch Image Models (EfficientNet architectures)
facenet-pytorch>=0.2.2  # MTCNN face detection

# Utilities
requests>=2.32.0
tqdm>=4.66.0
PyYAML>=6.0
python-dateutil>=2.9.0
